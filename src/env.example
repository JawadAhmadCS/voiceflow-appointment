# Copy to project root as `.env` / `.env.local` (never commit secrets).
# Optional: without DATABASE_URL, bookings stay in memory per Vercel instance.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
